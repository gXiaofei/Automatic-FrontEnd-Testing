import Util from './utils';
let util = null;

beforeAll(() => {
    util = new Util();
})

test('测试 a 方法', () => {
    // expect(util.a(1,2)).toBe(3)
})


test('测试 b 方法', () => {
    // expect(util.b(1,2)).toBe(3)
})

